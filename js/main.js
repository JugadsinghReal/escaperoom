const btnRestart = document.querySelector(".restart");

// Hier ga je terug naar het startscherm en kan je opnieuw beginnen
btnRestart.addEventListener("click", function() {
    window.location.href = ("../index.html");
});